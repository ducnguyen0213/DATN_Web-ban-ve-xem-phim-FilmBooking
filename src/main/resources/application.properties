spring.mvc.view.prefix:/WEB-INF/VIEW/
spring.mvc.view.suffix:.jsp
#
server.port=8080
# connect database
spring.datasource.url=jdbc:mysql://localhost:3306/datn
spring.datasource.username=root
spring.datasource.password=123456
spring.datasource.hikari.keepalive-time=300000
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.idle-timeout=30000
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.show-sql=false
#Hibernate Properties
#The SQL dialect makes Hibernate generate better SQL for the chosen database
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5InnoDBDialect
#Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto=none
logging.level.org.hibernate=ERROR

server.servlet.session.timeout=1800
#server.tomcat.relaxed-query-chars={,},[,]
spring.thymeleaf.enabled=true
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html

#spring.web.resources.staticLocations = classpath:/META-INF/resources/,classpath:/resources/,classpath:/static/,classpath:/public/,classpath:/templates/

# VNPAY - URL này PHẢI được đăng ký tại https://sandbox.vnpayment.vn (Cài đặt > URL Return / Domain)
# Nếu dùng localhost bị lỗi "Website chưa được phê duyệt", dùng ngrok (ngrok http 8080) rồi điền URL ngrok vào đây
vnpay.return-url-base=http://localhost:8080

# Tắt Flyway — bảng đã tạo thủ công, không cần migration tự động
spring.flyway.enabled=false

# Base URL dùng trong email reset mật khẩu (link trong mail)
app.base-url=http://localhost:8080

# Spring Mail (ví dụ Gmail - cần bật "Ứng dụng kém an toàn" hoặc dùng App Password)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=ducnguyen1302cat@gmail.com
spring.mail.password=vtzo bcyi iefk qtvj
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
# Email nhận thông báo đơn chờ (admin)
app.mail.admin-to=ducnguyen1302cat@gmail.com